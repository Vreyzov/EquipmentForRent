<Window x:Class="EquipmentForRent.DateTimePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EquipmentForRent"
        mc:Ignorable="d"
        Title="DateTimePickerWindow" Height="400" Width="400">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="https://img.freepik.com/premium-vector/white-backdrop-beautiful-abstract-background_36991-16.jpg" 
                       Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Надпись для выбора даты -->
        <TextBlock Text="Выберите дату, на которую хотите продлить аренду" 
                   FontSize="18" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   Margin="0,20,0,0"/>

        <!-- Развернутый календарь -->
        <Calendar x:Name="Calendar" HorizontalAlignment="Center" VerticalAlignment="Center" 
                  SelectedDate="{Binding SelectedDate}" Margin="0,30,0,0"/>

        <!-- Кнопка OK для подтверждения изменений -->
        <Button Content="OK" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Width="75" Click="OkButton_Click"/>

        <!-- Кнопка Отмена для отмены изменений -->
        <Button Content="Отмена" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Width="75" Click="CancelButton_Click"/>
    </Grid>
</Window>
